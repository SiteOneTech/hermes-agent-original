---
project_id: zeus-alpha-research-ledger-core
phase: local_advisory_ledger_v1
status: g1_rebaseline
validated: yes
reviewed: yes
reviewed_by: solution-architect
review_evidence: factory_gate_794
reviewed_candidate_sha: c81547062c5362a7be6f5a1bb2ef9612b29bac9c
reviewed_candidate_pr: https://github.com/SiteOneTech/hermes-agent-original/pull/36
reviewed_source_gate: factory_gate_790
reviewed_source_sha: 2476e978c545e24b18ee48844b24eb8c58245ab4
---

# SECURITY GATES

`DATABASE_AND_RUNTIME_CONTRACT.md` §§1–5 is the detailed acceptance contract. This gate lists the mandatory pass/fail evidence, not a weaker alternate design.

## Least-privilege gate
- Migration verifies all exact role attributes, zero role memberships, search path, per-object grants, `program_create`/`source_submit` leaf allowlist, `PUBLIC` revocations and default privileges from contract §1.
- A transaction as `alpha_research_runtime` proves each allowlisted operation works and every named direct-SQL denial in contract §1 fails. Catalog assertions prove no DML grant in other user schemas, no `PUBLIC` privilege in `alpha_research`, and the two lifecycle functions' `agent_admin` ownership, `SECURITY DEFINER`, fixed safe search path, exact signatures and execution grants.
- Direct lifecycle tests prove every permitted source→target edge and changed-column set, and deny direct update, unlisted edge, wrong/cross-owner actor, assumed role, mutable search path and all forbidden definer capabilities.
- Dedicated role secret is Infisical-only. Resolver accepts only the exact local DSN shape in contract §4; missing reference/role/allowlist fails before toolset or scheduler activation.

## Source/provenance gate
- Migration and handlers enforce the exact source classes, immutable persisted `source_reference`/`terms_evidence_reference`, terms states, freshness modes, max-age bounds, timestamp predicate, approval/revision provenance and uniqueness/supersession contract in §2.
- Direct SQL and handler suites execute every §2 negative case: source state combinations, immutable-reference mutations including admin attempts, incomplete/non-admin approval provenance, revision-audit mutation, stale/future/malformed time, duplicate, append-only evidence mutation/delete and lineage violation.
- Raw restricted content is never sent to generic memory or an external service. Provider fetch/parse code remains out of tree.

## Typed research-only gate
- `alpha_cards`, `research_reviews`, `inert_handoff_packages` and every JSON envelope implement the exact typed tuple/default/check/immutability contract in §3.
- Direct SQL and handler suites reject omitted/mutated/wrong tuples and every named validated/advice/approval/promotion/operation/activation/unknown field across all three persisted carriers and handoffs; field-by-field `alpha_card_create`, fixed `handoff_list` objects, payload/envelope count and type/bound checks are exact.
- Synthetic-canary tests prove the generic reference-only secret contract: no resolver/DSN/environment secret material appears in a persisted row, output, log, error, trace/span/metric or scheduler status, while fixed redacted errors remain deterministic.

## No-egress/tool isolation gate
- Default toolsets contain none of the ten handlers; the non-default leaf contains exactly `alpha_research_status`, `program_create`, `source_submit`, `evidence_record`, `alpha_card_create`, `alpha_card_review`, `cycle_start`, `cycle_close`, `inert_handoff_prepare`, `handoff_list`.
- Static scan covers every added/replacement line in every ALR-added or ALR-modified implementation diff file from a recorded, ancestor-verified base SHA; no selected-path exception is allowed. It rejects every §4 banned import/SQL pattern and unscannable changed file.
- Runtime harness executes all handlers and scheduler registration/run with outbound socket/HTTP/subprocess denial. Any attempt fails the test; only the exact local Postgres DSN is permitted.

## R2u documentation-status repair gate
- R2u modifies only project-local Markdown artifacts under `factory/projects/zeus-alpha-research-ledger-core/` and imports no runtime/control-plane code, provider dependency, network client, credential path, message connector, deployment path, trading/risk/paper/live behavior or external runtime authority.
- The reviewed G1 markers cite PR #36 head `c81547062c5362a7be6f5a1bb2ef9612b29bac9c` and Factory gate `794`; they are documentation-readiness evidence only and do not grant implementation permission outside the downstream task gates above.

## R2v control-plane repair gate
- R2v is limited to Factory control-plane/document-status and completion enforcement. It must not add runtime provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, or product ledger implementation.
- The configured-base-ref status reader may inspect committed Git blobs from the verified origin base ref but must not checkout, fast-forward, merge, or mutate the primary checkout.
- The no-auto-integration guard is a fail-closed safety boundary for projects with `factory_auto_integration_forbidden=true`; such projects require PR-first independent QA rather than Factory branch-to-base integration.

## R2ah documentation-only security gate
- R2ah is limited to current-origin reviewed-marker/index documentation repair under `factory/projects/zeus-alpha-research-ledger-core/` and must not add or alter runtime provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, or direct Factory DB writes.
- The exact base for this repair is `origin/main` `1b6bc0f65d3ad49845d20e056203e3b3702ac2a7`; the fresh PR is a review handoff only and does not authorize merge, deploy, external runtime execution, or downstream ALR implementation.

## R2c2 documentation-only security gate
- R2c2 is limited to autonomous canonical G1 `document_status` evidence repair under `factory/projects/zeus-alpha-research-ledger-core/` and must not add or alter runtime provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, or direct Factory DB writes.
- The exact base for this repair is `origin/main` `dbde1790f8d45f111bc69b3491a1862eafb29fa2`; the fresh PR is a review handoff only and does not authorize merge, deploy, external runtime execution, or downstream ALR implementation.

## R2c4 control-plane source-selection security gate
- R2c4 is limited to Factory `document_status` source identity and evidence docs; it must not add or alter runtime provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, or direct Factory DB writes.
- The configured source for this repair is `origin/main` `2a32066398d500d6dac071bd7f2184d47bb3bcb4`; stale primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is recorded only as rejected identity evidence and never as canonical readiness authority.
- Fail-closed behavior is mandatory when configured base identity cannot be verified. No fallback can clear G1 blockers from arbitrary worktree, PR, candidate, metadata, or stale-primary state.

## R2c5 documentation-only independent review security gate
- R2c5 is limited to project-local documentation/index/review-evidence under `factory/projects/zeus-alpha-research-ledger-core/` plus the approved Factory status/gate-record CLI evidence; it must not add or alter runtime provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, the primary checkout, or direct Factory DB writes.
- The exact base for this review/repair is `origin/main` `91aa62b11f02f69d88f7d8c18c30033edb4b7355`; the fresh PR is a review handoff only and does not authorize merge, deploy, credential change, external runtime execution, or downstream ALR implementation.
- The live runtime mismatch (stale primary checkout at `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` running the pre-R2v resolver) is evidence to document and route as bounded technical rework, never authority to mutate the primary checkout from this increment.

## R2c6 bounded current-origin resolver security gate
- R2c6 is limited to Factory control-plane G1 `document_status` resolution/readback and project-local evidence docs. It must not add or alter runtime provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, production promotion, external runtimes, or direct Factory DB writes.
- The resolver may inspect committed Git blobs at the verified configured base ref and may read a separately checked-out candidate only after exact base branch/commit/ancestry, PR head, independent review, and clean artifact-state verification. It must not checkout, fast-forward, merge, write, or otherwise mutate the primary checkout.
- Fail-closed behavior is mandatory for unavailable paths, dirty or untracked artifacts, malformed candidate metadata, stale/base-mismatched candidates, missing independent review, and unreviewed document markers. Such candidates cannot clear G1 blockers.
- The exact current configured base for this repair is `origin/main` `40a188b23a384901f983e4d959d3ebbecf50b318`; primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is recorded only as rejected identity evidence.

## R2am stale-primary Factory tick source-resolution security gate
- R2am is limited to Factory CLI/dashboard tick source resolution and project-local evidence docs. It must not add or alter runtime provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, external runtimes, or direct Factory DB writes.
- Project tick must execute only the orchestrator script in the running `hermes_cli.factory` source tree and must force the subprocess import path to that same source root. It must not trust `~/.hermes/scripts/factory_orchestrator_tick.py` when that wrapper can point to a stale primary checkout.
- Fail-closed behavior is mandatory when the running Factory source provenance is malformed or when the source tree lacks the tick script. No fallback may execute a stale profile wrapper or clear G1 blockers from stale-primary code.
- The exact current configured base for this repair is `origin/main` `b525254809fba0ad46e6b7e9405778c44e64bae9`; primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is recorded only as rejected identity evidence.

## G1 document-status technical recovery security gate
- This recovery is limited to current-origin documentation/provenance under `factory/projects/zeus-alpha-research-ledger-core/`. It must not add or alter runtime provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, external runtimes, primary checkout state, or direct Factory DB writes.
- The exact current configured base for this recovery is `origin/main` `139df9ae49137bb4b16152550d53d385310de3b6`; primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is recorded only as rejected identity evidence.
- If stale persisted anomaly metadata remains after current row-level `document_status` is non-blocking, the only authorized follow-up is a bounded Factory control-plane repair through reviewed code/PR/gates or approved Factory CLI gate evidence. No direct SQL, credential access, deploy, runtime call, or primary checkout fast-forward is authorized by this documentation task.

## R2aj isolated current-base G1 documentation evidence recovery security gate
- R2aj is limited to current-base documentation/provenance under `factory/projects/zeus-alpha-research-ledger-core/`. It must not add or alter runtime provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, external runtimes, primary checkout state, or direct Factory DB writes.
- The exact current configured base for this recovery is `origin/main` `bf422968f9ea73d70d4ac1e8b8bae4af644ce079`; primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is recorded only as rejected identity evidence.
- Stale PR #44 and obsolete PR #20 checkout metadata are not current-base evidence. If stale persisted anomaly or dispatch-preflight projection metadata remains after row-level `document_status` is non-blocking, the only authorized follow-up is a bounded Factory control-plane repair through reviewed code/PR/gates or approved Factory CLI gate evidence. No direct SQL, credential access, deploy, runtime call, primary checkout fast-forward, external connector, messaging action, trading, risk mutation, paper/live activation, or self-approval is authorized by this documentation task.

## R2ao current-origin G1 control-plane projection repair security gate
- R2ao is limited to Factory control-plane projection/readiness logic and project-local evidence docs. It must not add or alter Agent Core ledger runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, external runtimes, primary checkout state, or direct Factory DB writes.
- The exact configured base for this repair is `origin/main` `4a0a6bbaea3b1acaf8e83084c058b831d865d8c4`; primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` remains rejected identity evidence and must not be mutated.
- Stale PR #20 `metadata.g1_documentation_checkout` may be removed from the persisted projection only by the reviewed reconciler path when current required-document rows are non-blocking. It must not be manually removed through direct SQL, and stale metadata must never clear blockers when current rows still block.
- Delivery is a Zeus-signed `agent:zeus` PR plus independent exact-SHA security review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, or self-approval is authorized.

## R2ap current-origin G1 document validation recovery security gate
- R2ap is limited to current-origin documentation/provenance under `factory/projects/zeus-alpha-research-ledger-core/`. It must not add or alter Agent Core ledger runtime code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, external runtimes, primary checkout state, or direct Factory DB writes.
- The exact current configured base for this recovery is `origin/main` `3e32da02c218e06a69b851641b2d454113654378`; primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is recorded only as rejected identity evidence and must not be mutated.
- Historical `unvalidated_required_docs` and `missing_or_unindexed_docs` evidence must remain stale/audit provenance when current required-document rows are non-blocking and active metadata reports `reconciliation_anomalies=[]` from `current_document_status`; it must not be used to clear or recreate blockers through direct SQL or self-approval.
- Delivery is a Zeus-signed `agent:zeus` PR plus independent exact-SHA quality review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, or no external runtime execution is authorized.

## R2at current-origin documentation validation security gate
- R2at is limited to current-origin documentation/provenance under `factory/projects/zeus-alpha-research-ledger-core/`. It must not add or alter Agent Core ledger runtime code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, product ledger implementation, external runtimes, primary checkout state, or direct Factory DB writes.
- The exact current configured base for this recovery is `origin/main` `a41acdc4820b92a31b7d42d9a9c28e95b875a3d1`; primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is recorded only as rejected identity evidence and must not be mutated.
- Historical `unvalidated_required_docs` and `missing_or_unindexed_docs` evidence from events `194478`, `194477`, and `194474` must remain stale/audit provenance when current required-document rows are non-blocking and active metadata reports `reconciliation_anomalies=[]` from `current_document_status`; it must not be used to clear or recreate blockers through direct SQL, self-approval, primary-checkout mutation, or no-auto-merge bypass.
- Delivery is a Zeus-signed `agent:zeus` PR plus independent exact-SHA quality review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, or no external runtime execution is authorized.

## R2au current-origin G1 projection repair security gate
- R2au is limited to Factory control-plane document-status projection/readback logic and project-local evidence docs. It must not add or alter Agent Core ledger runtime/product code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, or direct Factory DB writes.
- The exact current configured base for this repair is `origin/main` `2b53ee0f14491ff43da7683d475654a03af5d678`, and that base contains R2at commit `d4ac6d89994adf823bb50b79afe5a39fd204fdfd`. Primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is rejected identity evidence only and must not be mutated.
- Stale required-doc anomaly strings in historical events/tasks remain immutable audit history, but active project status must not re-present them as current required-doc readiness after configured-base rows are clean. The repair must use reviewed code/tests/PR/gates, not direct SQL cleanup.
- Delivery is a Zeus-signed `agent:zeus` PR plus independent exact-SHA review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, or external runtime execution is authorized.

## R2av current-origin G1 status projection verification security gate
- R2av is limited to isolated current-origin Factory status verification and project-local evidence docs. It must not add or alter Agent Core ledger runtime/product code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, or direct Factory DB writes.
- The exact current configured base for this verification is `origin/main` `af9fa27eaaaa52ef173f1578fb7f572ce52cebc6`, the R2au PR #61 merge, which contains repair commit `1afd37a61a8d21af393e393cb77083adb25b41c7`. Primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is rejected identity evidence only and must not be mutated.
- Stale `reviewed=false` rows from the stale primary/runtime path or historical gate/event snapshots must remain audit evidence only and must not override current configured-base rows. Any future repair must use reviewed code/tests/PR/gates or approved Factory CLI control path, never direct SQL cleanup.
- Delivery is a Zeus-signed `agent:zeus` PR plus independent exact-SHA quality review and QA Guardian evidence. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, or external runtime execution is authorized.

## R2bb current-base G1 status projection / PR #63 evidence recovery security gate
- R2bb is limited to Factory CLI status-source provenance readback and project-local evidence docs. It must not add or alter Agent Core ledger runtime/product code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, or direct Factory DB writes.
- The exact current configured base for this repair is `origin/main` `b05afe59c88cfa7f7dbec0117603b2f052267ce0`, the PR #63/R2aw merge, which contains repair commit `dcd9c74f252d288269d746ab59079a0221de7a46`. Primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is rejected identity evidence only and must not be mutated.
- Factory status JSON may add deterministic source-root provenance fields but must not treat stale primary checkout, historical events, stale assignment summaries, or PR metadata as current G1 readiness authority. Clean current configured-base rows remain the readiness source; unavailable/non-JSON delegated status output falls back to the prior stdout/stderr passthrough rather than fabricating readiness.
- Delivery is a Zeus-signed `agent:zeus` PR plus independent exact-SHA quality review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, direct SQL, primary-checkout mutation, or external runtime execution is authorized.

## R2BJ bounded canonical G1 documentation/index recovery security gate
- R2BJ is limited to current-base project-local documentation/index/evidence. It must not add or alter Agent Core ledger runtime/product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, or direct Factory DB writes.
- The exact current configured base for this repair is `origin/main` `b503ba3b57fd606956d0ebf925c83eda253bdcc5`. Primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is rejected identity evidence only and must not be mutated.
- Historical `unvalidated_required_docs` and `missing_or_unindexed_docs` evidence from events/gates must remain audit/projection provenance when current required-document rows are non-blocking and active metadata reports `reconciliation_anomalies=[]` from `current_document_status`. It must not be used to self-approve, direct-SQL mutate, clear or recreate blockers, mutate primary checkout, or bypass PR-first/no-auto-merge policy.
- Delivery is a Zeus-signed `agent:zeus` PR plus independent exact-SHA quality review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, direct SQL, primary-checkout mutation, or external runtime execution is authorized.

## R2cm review-state provenance repair security gate
- R2cm is limited to current-base project-local documentation/provenance. It must not add or alter Agent Core ledger runtime/product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, G1 reviewed frontmatter markers, or direct Factory DB writes.
- The exact current configured base for this repair is `origin/main` `0ecd9019ba8ec111aaead60a911c9accd854f731`. Primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is rejected/stale identity evidence only and must not be mutated.
- Authorized Factory DB readback is the canonical `/home/jean/Projects/hermes-agent-original/venv/bin/hermes factory status zeus-alpha-research-ledger-core --json` plus the allowed worktree diagnostic `/home/jean/Projects/hermes-agent-original/venv/bin/python3 -m hermes_cli.main factory status zeus-alpha-research-ledger-core --json`; no direct SQL / psql / psycopg2 / ad-hoc database script is authorized.
- Security invariant: do not turn R2cl task/run completion into review approval. The R2cl terminal quality-review path exhausted on MiniMax HTTP 429, so any green R2cm outcome must come from a fresh independent exact-SHA PR review. If that review fails/rate-limits, the secure state is pending/failed, not auto-green.
- Delivery is a Zeus-signed `agent:zeus` PR plus independent exact-SHA quality review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, direct SQL, primary-checkout mutation, or external runtime execution is authorized.

## R2cn bounded canonical G1 docs gate security gate
- R2cn is limited to current-base project-local documentation/provenance. It must not add or alter Agent Core ledger runtime/product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, G1 reviewed frontmatter markers, or direct Factory DB writes.
- The exact current configured base for this repair is `origin/main` `fa24950a228f28d5106ee2125d42045e872f9504`. Primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is rejected/stale identity evidence only and must not be mutated.
- Authorized Factory DB readback/control is limited to the canonical venv CLI invocation from the assigned worktree for `factory status` and `factory project resolve-state`; no direct SQL / psql / psycopg2 / ad-hoc database script is authorized.
- Security invariant: stale historical `unvalidated_required_docs` events/gates/tasks must not override current configured-base document rows when all required G1 rows are reviewed and non-blocking. Any remaining non-G1 technical rework, such as R2ac, stays separate and must not be used to re-create a G1 docs blocker.
- Delivery is a Zeus-signed `agent:zeus` PR plus independent exact-SHA quality review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, direct SQL, primary-checkout mutation, or external runtime execution is authorized.

## R2ai-R2 non-destructive current-origin G1 documentation recovery security gate
- R2ai-R2 is limited to current-origin project-local documentation/evidence under `factory/projects/zeus-alpha-research-ledger-core/`. It must not add or alter Agent Core ledger runtime/product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, G1 reviewed frontmatter markers, or direct Factory DB writes.
- The exact current configured base for this repair is `origin/main` `6c07c2fee59679a5b0063e635f0332895dbb3ec5`. Primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is rejected/stale identity evidence only and must not be mutated.
- Non-destructive delivery invariant: the assigned branch `factory/zeus-alpha-research-ledger-core/inc-017-r2ai-r2-non-destructive-current` must not pre-exist on origin; delivery uses a normal fresh-branch push and no existing remote ref is force-pushed, reset, or rewritten (the R2ai-R1 force-push attempt was blocked by the Hermes guard and is recorded as historical evidence only).
- Authorized Factory DB readback/control is limited to the canonical venv CLI invocation from the assigned worktree for `factory status` and `factory gate record`; no direct SQL / psql / psycopg2 / ad-hoc database script is authorized.
- Security invariant: stale historical `unvalidated_required_docs` task/event/gate records must not override current configured-base document rows when all required G1 rows are reviewed and non-blocking. The remaining stale task-level metadata on the blocked R2ai task is a control-plane projection artifact; it is recorded as the exact bounded follow-up Factory task `zeus-alpha-research-ledger-core-r2ai-r2-canonical-active-metadata-anomaly-repair` and is not silently cleared, self-approved, or SQL-mutated by this documentation task.
- The designated independent security review for the current candidate remains pending (gate `896` failed); this documentation repair does not re-issue `reviewed`, does not change any required document's `reviewed` field, and does not self-approve.
- Delivery is a Zeus-signed non-draft `agent:zeus` PR plus independent exact-SHA quality review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, direct SQL, primary-checkout mutation, or external runtime execution is authorized.

## R2ap PR #72 residual G1 task metadata reconciliation security gate
- R2ap-PR72 is limited to current-origin project-local documentation/evidence under `factory/projects/zeus-alpha-research-ledger-core/`. It must not add or alter Agent Core ledger runtime/product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, G1 reviewed frontmatter markers, or direct Factory DB writes outside the explicitly allowed `factory gate record` evidence path.
- The exact configured base for this reconciliation is `origin/main` `c31e937111bba64e478d3c319e896774bf09e40e`; PR #72 exact head is `3311b82ee1a29043039003e94582509bb8b89895`. Primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` is rejected/stale identity evidence only and must not be mutated.
- Security invariant: stale task-level `unvalidated_required_docs` metadata on R2ai/R2ae must not override current configured-base G1 rows when all required rows are reviewed and non-blocking. Historical task/event/gate metadata remains fail-closed audit/control-plane evidence until an explicitly authorized canonical closure/reconciler path changes it.
- Closure invariant: `factory gate record` may record evidence but must not be treated as task closure or supersession. This run must not invoke `factory task close` because the hard DB-write allowlist did not authorize it; if closure is needed, it requires a separately authorized canonical close/supersede action with source-backed evidence.
- Delivery remains Zeus-signed PR-first plus independent exact-SHA quality review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, direct SQL, primary-checkout mutation, force-push/ref rewrite, or external runtime execution is authorized.

## R2ax current-origin Factory CLI G1 recovery dispatch security gate
- R2ax is limited to Factory CLI source-selection/provenance and project-local evidence docs. It must not add or alter Agent Core ledger runtime/product code, ALR product code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, G1 reviewed frontmatter markers, or direct Factory DB writes outside the explicitly allowed `factory gate record` evidence path.
- The exact configured base for this repair is `origin/main` `e7e216272ea64a83351ae38f27688ecda47cbbbf`. Primary checkout HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` remains rejected/stale identity evidence only and must not be mutated.
- Security invariant: when the CLI is invoked from an assigned isolated current-origin worktree and the running module is stale, `factory project resolve-state` and related recovery project actions must execute from the complete current-origin source tree with matching `cwd`/`PYTHONPATH`. A delegated failure must return failure and must not fall back to stale primary/backend code or stale task metadata.
- Authorized live Factory DB readback in this run is limited to `factory status` and `factory gate record`; `resolve-state`/`tick` are validated by behavioral tests because they may mutate control-plane state. This limitation must not be reported as ALR-020/product readiness.
- Delivery remains Zeus-signed PR-first plus independent exact-SHA review. No merge, deploy, credential access, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, direct SQL, primary-checkout mutation, force-push/ref rewrite, external runtime execution, or ALR-020/product dispatch is authorized.

## R2az non-destructive current-base G1 evidence recovery security gate
- R2az is documentation/evidence only and may change only project-local files under `factory/projects/zeus-alpha-research-ledger-core/`. It must not add or alter Agent Core ledger runtime/product code, ALR product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, task status, G1 reviewed frontmatter markers, or direct Factory DB writes outside the explicitly allowed `factory gate record` evidence path.
- The exact configured base for this repair is `origin/main` `3b7bc91f2ee1ef603bb512d147c692568c1b465f`. Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-017-r2az-non-destructive-current-bas` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-017-r2az-non-destructive-current-bas` starts with `HEAD=origin/main=merge-base`; the assigned remote branch must be absent before normal push. Stale R2ai/R2ae/R2ac remote refs and PRs are historical evidence only and must not be force-pushed, reset, rebased, reused, or otherwise mutated.
- Security invariant: current Factory status from the assigned worktree is authoritative for G1 rows when it reports source roots equal to the worktree, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, 14/14 required rows reviewed/non-blocking, and stale primary rejected. Stale `project_reconciled` / `dispatch_preflight_denied` events and old blocked R2ai/R2ae task metadata must remain identified as residual projection evidence, not as current document-content failure.
- Authorized live Factory DB interaction in this run is limited to `factory status` and `factory gate record`. No `factory task close`, `factory project resolve-state`, direct SQL, `psql`, `psycopg2`, or ad-hoc DB script is authorized for task-state mutation by this worker.
- Delivery remains Zeus-signed PR-first plus independent exact-SHA review. No merge, deploy, credential access/change, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, direct SQL, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.

## R2ba current-base G1 independent review and review-state repair security gate

- R2ba is documentation/review-evidence only and may change only project-local files under `factory/projects/zeus-alpha-research-ledger-core/`. It must not add or alter Agent Core ledger runtime/product code, ALR product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, task status, G1 reviewed frontmatter markers, or direct Factory DB writes outside the explicitly allowed `factory status` / `factory gate record` evidence path.
- The exact configured base for this review/repair is `origin/main` `756ac62a4c69278216b2b7e66b34e6f11ad54c29` (R2az PR #76 merge; R2az head `bb99d21547ff14fabe175741d5a5e400c99c922e`). Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-001-r2ba-current-base-g1-independent` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-001-r2ba-current-base-g1-independent` starts with `HEAD=origin/main=merge-base`; the assigned remote branch was absent before normal push and no existing remote ref is force-pushed, reset, or rewritten.
- Security invariant: current Factory status from the assigned worktree is authoritative for G1 rows when it reports source roots equal to the worktree, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, 14/14 required rows reviewed/non-blocking, and stale primary `4eb87e4cd4…` rejected. The ten-document `reviewed=false` projection named in the task description is stale-primary/historical evidence and must not override current configured-base rows. The docs-only security assessment for this candidate is recorded as canonical Factory gate `916`; the designated security-reviewer role remains the owner of product-level ALR-063 security gates.
- Authorized live Factory DB interaction in this run is limited to `factory status` and `factory gate record`. No `factory task close`, `factory project resolve-state`, direct SQL, `psql`, `psycopg2`, or ad-hoc DB script is authorized for task-state mutation by this worker; obsolete blocked R2ai/R2ae/R2ac rows stay fail-closed.
- Delivery remains Zeus-signed PR-first plus independent exact-SHA review. No merge, deploy, credential access/change, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, direct SQL, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.

## R2bl non-destructive canonical G1 evidence repair security gate

- R2bl is documentation/evidence only and may change only project-local files under `factory/projects/zeus-alpha-research-ledger-core/`, including the deterministic project-local validator `validate_r2bl_g1_evidence.py`. It must not add or alter Agent Core ledger runtime/product code, ALR product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, task status, G1 reviewed frontmatter markers, or direct Factory DB writes outside the explicitly allowed `factory status` / `factory gate record` evidence path.
- The exact configured base for this repair is `origin/main` `faddaf5afb4c1754e03d8c97dd6706353b5b0865`. Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-001-r2bl-non-destructive-canonical-g` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-001-r2bl-non-destructive-canonical-g` starts with `HEAD=origin/main=merge-base`; the assigned remote branch must be absent before normal push and no existing remote ref may be force-pushed, reset, or rewritten.
- Security invariant: current Factory status from the assigned worktree is authoritative for G1 rows when it reports source roots equal to the worktree, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, 14/14 required rows reviewed/non-blocking, and stale primary `4eb87e4cd4…` rejected. Stale structured R2ai/R2ae task metadata and historical `unvalidated_required_docs` / `missing_or_unindexed_docs` events must remain audit/projection evidence, not current document-content failure or authority for self-approval/direct SQL cleanup.
- Authorized live Factory DB interaction in this run is limited to `factory status` and `factory gate record`. No `factory task close`, direct SQL, `psql`, `psycopg2`, ad-hoc DB script, deploy, credential access/change, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.
- Delivery remains Zeus-signed PR-first at `https://github.com/SiteOneTech/hermes-agent-original/pull/78` plus independent exact-SHA quality and security review. The exact final PR head SHA belongs in the PR body and Factory gate records because a commit cannot contain its own SHA.

## R2bm canonical G1 docs gate source-root recovery security note

- R2bm reached `origin/main` as merge commit `9ebaa9e7b44c61bb871ca4da0a838c52e62666b2` from PR #79 head `06051d990821bc7127004313ca3458e0394832d8`, with quality gate `925` and implementation gate `926`. This source-root control-plane evidence did not authorize runtime/product code, ALR product dispatch, direct SQL, primary-checkout mutation, external runtime execution, credential access/change, or deployment.

## R2bn canonical G1 review-state source-root repair security gate

- R2bn is documentation/evidence only and may change only project-local files under `factory/projects/zeus-alpha-research-ledger-core/`, including the deterministic project-local validator `validate_r2bn_g1_evidence.py`. It must not add or alter Agent Core ledger runtime/product code, ALR product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, task status, G1 reviewed frontmatter markers, or direct Factory DB writes outside the explicitly allowed `factory status` / `factory gate record` evidence path.
- The exact configured base for this repair is `origin/main` `9ebaa9e7b44c61bb871ca4da0a838c52e62666b2`. Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-018-r2bn-canonical-g1-review-state-s` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-018-r2bn-canonical-g1-review-state-s` starts with `HEAD=origin/main=merge-base`; no existing remote ref may be force-pushed, reset, or rewritten.
- Security invariant: current Factory status from the assigned worktree is authoritative for G1 rows when it reports source roots equal to the worktree, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, 14/14 required rows reviewed/non-blocking, and stale primary `4eb87e4cd4…` rejected. Stale structured R2ai/R2ae task metadata, gates `927`/`928`, and historical `unvalidated_required_docs` / `missing_or_unindexed_docs` events must remain audit/projection evidence, not current document-content failure or authority for self-approval/direct SQL cleanup.
- Authorized live Factory DB interaction in this run is limited to `factory status` and `factory gate record`. No `factory task close`, `factory project resolve-state`, direct SQL, `psql`, `psycopg2`, ad-hoc DB script, deploy, credential access/change, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.
- Delivery remains Zeus-signed PR-first at `https://github.com/SiteOneTech/hermes-agent-original/pull/80` plus independent exact-SHA quality review. The exact final PR head SHA belongs in the PR body and Factory gate records because a commit cannot contain its own SHA.

## R2bn post-merge security note

- R2bn reached `origin/main` as merge commit `0db9bed7ed9e8ec4dbefda41f95a335ab82fbbc0` from PR #80 head `5dcf7d14746457148b045e2ed94aed6114054e6d` on base `9ebaa9e7b44c61bb871ca4da0a838c52e62666b2`, with independent quality gate `929` by `quality-reviewer`. This predecessor evidence is documentation/control-plane readback only and does not authorize runtime/product code, ALR product dispatch, direct SQL, primary-checkout mutation, external runtime execution, credential access/change, or deployment.

## R2ct bounded canonical G1 documentation validation security gate

- R2ct is documentation/evidence only and may change only project-local files under `factory/projects/zeus-alpha-research-ledger-core/`, including the deterministic project-local validator `validate_r2ct_g1_evidence.py`. It must not add or alter Agent Core ledger runtime/product code, ALR product code, Factory runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, task status, G1 reviewed frontmatter markers, or direct Factory DB writes outside the explicitly allowed `factory status` / `factory gate record` evidence path.
- The exact configured base for this repair is `origin/main` `0db9bed7ed9e8ec4dbefda41f95a335ab82fbbc0`. Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-019-r2ct-bounded-canonical-g1-docume` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-019-r2ct-bounded-canonical-g1-docume` starts with `HEAD=origin/main=merge-base`; no existing remote ref may be force-pushed, reset, or rewritten.
- Security invariant: current Factory status from the assigned worktree is authoritative for G1 rows when it reports source roots equal to the worktree, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, 14/14 required rows reviewed/non-blocking, and stale primary `4eb87e4cd4…` rejected. R2ct keeps candidate readiness separate from primary origin/main readiness: the exact final R2ct PR head must be bound by an independent quality gate, while current configured-base rows prove only primary origin/main document readiness. PR #80 head `5dcf7d14746457148b045e2ed94aed6114054e6d` and quality gate `929` are predecessor evidence.
- Authorized live Factory DB interaction in this run is limited to `factory status` and `factory gate record`. No `factory task close`, `factory project resolve-state`, direct SQL, `psql`, `psycopg2`, ad-hoc DB script, deploy, credential access/change, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.
- Delivery remains Zeus-signed PR-first at `https://github.com/SiteOneTech/hermes-agent-original/pull/82` plus independent exact-SHA quality review. The exact final PR head SHA belongs in the PR body and Factory gate records because a commit cannot contain its own SHA.

## R2cv current-origin G1 documentation validation recovery security gate

- R2cv may change only Factory CLI resolver source-root delegation code, focused tests, and project-local evidence docs. It must not add or alter ALR product/runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, task status, G1 reviewed frontmatter markers, or direct Factory DB writes outside sanctioned Factory CLI evidence.
- The exact configured base for this repair is `origin/main` `12f5696882f04ee24b6fd1bf957abafaf76eab31`. Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-019-r2cv-current-origin-g1-documenta` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-019-r2cv-current-origin-g1-documenta` starts with `HEAD=origin/main=merge-base`; no existing remote ref may be force-pushed, reset, or rewritten.
- Security invariant: resolver-class `factory project` actions may delegate from a stale strict-ancestor primary root only to a complete clean worktree whose HEAD exactly matches the configured base ref. Dirty configured-base sources plus ahead/diverged running branches remain local/fail-closed. `resume` is intentionally excluded from configured-base fallback so this repair does not broaden runtime/product dispatch.
- Authorized live Factory DB interaction remains bounded to sanctioned CLI readbacks and gate evidence; no direct SQL, `psql`, `psycopg2`, ad-hoc DB script, credential access/change, deploy, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.
- Delivery remains Zeus-signed PR-first plus independent exact-SHA quality review. The exact final PR head SHA belongs in the PR body and Factory gate records because a commit cannot contain its own SHA.

## R2db current-origin G1 reviewed-state PR recovery security gate

- R2db may change only Factory review-run terminalization code, focused tests, and project-local provenance. It must not add or alter ALR product/runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, G1 reviewed frontmatter markers, task status, or direct Factory DB writes outside the explicitly allowed `factory status` / `factory gate record` evidence path.
- The exact configured base for this repair is `origin/main` `18ef28e6845d2d15dd0ec3cd7d8bb9b7630b71cc`. Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-017-r2db-current-origin-g1-reviewed` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-017-r2db-current-origin-g1-reviewed` starts with `HEAD=origin/main=merge-base`; no existing remote ref may be force-pushed, reset, or rewritten.
- Security invariant: review-run terminal close must be fail-closed. Prompt-only `STATE: DONE` markers, empty reviewer output, HTTP-429/provider-failure transcripts, zero-tool review sessions, and gates scoped to the project or another task cannot mark this task reviewed. A positive review-run close requires a same-task passed Factory review gate before increment integration.
- Authorized live Factory DB interaction in this run is limited to `factory status` and `factory gate record`. No `factory task close`, direct SQL, `psql`, `psycopg2`, ad-hoc DB script, deploy, credential access/change, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.
- Delivery remains Zeus-signed PR-first plus independent exact-SHA quality/security review. The exact final PR head SHA belongs in the PR body and Factory gate records because a commit cannot contain its own SHA.

## R2dc bounded G1 reviewed-state recovery security gate

- R2dc may change only Factory review-run terminalization/reconcile recovery code, focused tests, and project-local provenance. It must not add or alter ALR product/runtime code, provider clients, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, G1 reviewed frontmatter markers, or direct SQL.
- The exact configured base for this repair is `origin/main` `c3c9332e7a5f0e3a41c49cfb0b190dfe16a8e12e` after fast-forwarding the assigned branch to current main. Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-016-r2dc-bounded-g1-reviewed-state-r` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-016-r2dc-bounded-g1-reviewed-state-r` starts final delivery with `HEAD=origin/main=merge-base=remote refs/heads/main`.
- Security invariant: actual provider/runtime HTTP 429 evidence (`provider response`, `RateLimitError`, `API call failed`, empty reviewer output, zero-tool transcripts, prompt-only terminal markers, or gates scoped to a project/different task) cannot mark a review accepted, cannot terminalize a review task as `done`, and cannot integrate source. Such provider/runtime failures requeue to `review_ready` for a distinct independent exact-SHA review. Normal reviewer prose that documents or quotes the prior 429-class condition is not itself runtime failure evidence and may close only when the same-task passed Factory review gate already exists. Retrospective false-terminal recovery is current-configured-base scoped and idempotent per recovered run id; historical integrated bases are not reopened by this path, while later false-terminal runs on the same task are still fail-closed and requeued.
- Authorized live Factory control evidence for this run is bounded to Factory CLI status/resolve-state readbacks and final gate recording; this run used `/tmp/r2dc-r3-status-before.json`, `/tmp/r2dc-r3-resolve-state.json`, and `/tmp/r2dc-r3-status-after.json` only. No `psql`, `psycopg2`, ad-hoc DB script, credential access/change, deploy, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.
- Delivery remains Zeus-signed PR-first plus independent exact-SHA security review. The exact final PR head SHA belongs in the PR body and Factory gate records because a commit cannot contain its own SHA.

## R2cx current-origin DOCUMENTATION_INDEX reviewed-state repair security gate

- R2cx may change only project-local documentation/provenance under `factory/projects/zeus-alpha-research-ledger-core/`. It must not add or alter Agent Core ledger runtime/product code, Factory runtime code, providers, migrations, tools, schedulers, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, G1 reviewed frontmatter markers, task status, or direct Factory DB writes outside sanctioned `factory status` / `factory gate record` evidence.
- The exact configured base for this repair is `origin/main` `b68ec8ad5cf986e5bf4900506820ca978ef0b0c0`, the PR #88 merge. Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-001-r2cx-current-origin-documentatio` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-001-r2cx-current-origin-documentatio` starts with `HEAD=origin/main=merge-base`.
- Security invariant: current Factory status from the assigned worktree is authoritative for G1 rows when it reports source roots equal to the worktree, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, 14/14 required rows reviewed/non-blocking, and `DOCUMENTATION_INDEX.md` reviewed/non-blocking. Stale primary-root status, stale prompt summaries, historical events/gates, and stale PR #44 must remain audit evidence only and must not override current configured-base rows.
- Authorized live Factory DB interaction in this run is limited to `factory status` and `factory gate record`. No `factory task close`, `factory project resolve-state`, direct SQL, `psql`, `psycopg2`, ad-hoc DB script, deploy, credential access/change, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.
- Delivery remains Zeus-signed PR-first plus independent exact-SHA quality review. The exact final PR head SHA belongs in the PR body and Factory gate records because a commit cannot contain its own SHA.

## R2dg bounded G1 exact-SHA independent-review dispatch recovery security gate

- R2dg may change only project-local documentation/provenance under `factory/projects/zeus-alpha-research-ledger-core/` plus sanctioned `factory status` / `factory gate record` evidence. It must not add or alter Agent Core ledger runtime/product code, Factory runtime code, providers, migrations, tools, schedulers, credential paths, messaging connectors, deployment behavior, trading/risk/paper/live behavior, external runtimes, primary checkout state, G1 reviewed frontmatter markers, task status, or direct Factory DB writes outside that evidence surface.
- The exact configured base for this recovery is `origin/main` `9ea2756e6bfbce9d07c7ce32319a8b64bd8cea15`. Assigned branch/worktree `factory/zeus-alpha-research-ledger-core/inc-001-r2dg-bounded-g1-exact-sha-indepe` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-001-r2dg-bounded-g1-exact-sha-indepe` starts with `HEAD=origin/main=merge-base`.
- Security invariant: current Factory status from the assigned worktree is authoritative for G1 rows when it reports source roots equal to the worktree, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, and 14/14 required rows reviewed/non-blocking. Stale primary-root status (`ac1fdb1605…`), stale prompt summaries, historical events/gates, and stale PR #44/#20 provenance must remain audit evidence only and must not override current configured-base rows.
- The independent security review remains **independently owned by `security-reviewer`** (this task's assigned reviewer profile and the separate ALR-063 security review task). This run records no security gate; it records only the G1 specification/quality review evidence (Factory gate `969`, reviewer `quality-reviewer`) and leaves security acceptance to the distinct security reviewer at the exact PR head.
- Authorized live Factory DB interaction in this run is limited to `factory status` and `factory gate record`. No `factory task close`, `factory project resolve-state`, direct SQL, `psql`, `psycopg2`, ad-hoc DB script, deploy, credential access/change, external connector, messaging operation, trading action, risk mutation, paper/live activation, self-approval, no-auto-merge bypass, primary-checkout mutation, force-push/ref rewrite, stale PR mutation, external runtime execution, or ALR-020/product dispatch is authorized.
- Delivery remains Zeus-signed PR-first plus independent exact-SHA quality and security review. The exact final PR head SHA belongs in the PR body and Factory gate records because a commit cannot contain its own SHA.

## Scheduler gate
- `agent_core.alpha_research.scheduler.enabled` is false absent explicit configuration.
- Registration and each invocation call the contract §5 verifier without cache. Tests cover every false/missing/failed/expired/wrong-commit readiness component and prove no batch read/run follows `scheduler_not_ready`.

## Failure behavior
All gate failures are structured local rejections. No fallback can enable an external operation, shared runtime role or stale scheduler.
