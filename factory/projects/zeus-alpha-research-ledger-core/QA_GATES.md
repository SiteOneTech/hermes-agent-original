---
project_id: zeus-alpha-research-ledger-core
phase: local_advisory_ledger_v1
status: g1_rebaseline
validated: yes
reviewed: yes
reviewed_by: solution-architect
review_evidence: factory_gate_794
reviewed_candidate_sha: c81547062c5362a7be6f5a1bb2ef9612b29bac9c
reviewed_candidate_pr: https://github.com/SiteOneTech/hermes-agent-original/pull/36
reviewed_source_gate: factory_gate_790
reviewed_source_sha: 2476e978c545e24b18ee48844b24eb8c58245ab4
---

# QA GATES

## ALR-010 documentary gate
- All 14 G1 documents plus G0, `REQUIREMENTS_TRACEABILITY.md`, `DATABASE_AND_RUNTIME_CONTRACT.md` and `G1_REVIEW.md` exist, are indexed, committed and independently PASS-reviewed; R2u binds the reviewed markers to PR #36 head `c81547062c5362a7be6f5a1bb2ef9612b29bac9c` and Factory gate `794`.
- `TASK_GRAPH.md` reconciliation matches current Factory task IDs, phases, owner/reviewer profiles, dependencies, branches, worktrees, PR-first metadata and the observed ALR-010-R1 direct integration event; before ALR-020, the incompatible bounded-local-sessions acceptance clause has the exact documented metadata correction/read-back evidence and v1 session/message exclusion remains intact.
- No document implies direct Vonash/trading authority, provider integration in core, or that the observed ALR-010-R1 direct Factory merge is approval/waiver/repeatable policy.

## ALR-020 database/role RED-GREEN gate
- Start with tests for every contract §1/§2/§3 constraint, trigger, lifecycle edge/changed-column/capability/catalog assertion, grant and named direct-SQL negative; observe RED before migration implementation.
- Green proves exact role properties/object grants, no `PUBLIC` access, source-reference/terms-reference immutability and approval/revision audit provenance, source freshness predicate, evidence/review append-only behavior, lineage integrity and all-card/review/handoff classification tuple enforcement.

## ALR-030 tools RED-GREEN gate
- Start with every-handler input/envelope/unknown-field/default-toolset/exact-`program_create`/`source_submit` leaf-allowlist/missing-secret negative. Observe RED before registration/handler implementation.
- Green proves field-bounded card input, fixed handoff-list object, unambiguous envelope/payload key counts, JSON envelope and exact no-advice contract in §3 plus all named prohibited labels/action fields and synthetic-secret redaction across output/log/error/tracing.

## ALR-050 scheduler/no-egress RED-GREEN gate
- Start with static scan failures for each banned dependency/SQL form in every added/replacement implementation diff line and runtime harness failures for every handler/scheduler path under interception.
- Green proves contract §4 all-ALR-modified-diff-line coverage, banned pattern rejection and exact-local-DSN-only DB connection.
- Start with config false/missing and every missing/failed/expired/wrong-commit readiness component; green proves no registration/no run and structured `scheduler_not_ready` under contract §5.

## Independent review gate
- **ALR-061** maps R1–R10/boundaries to exact implementation SHA, direct tests and scope limits.
- **ALR-062** verifies RED/GREEN artifacts, quality, test determinism and cleanup.
- **ALR-063** verifies the contract §1–§5 security proof against the exact candidate SHA.
- Each report cites the candidate SHA and creates bounded rework rather than broad approval.

## Live local gate
- Actual local Agent Core migration and dedicated-role tests run without secret output.
- Synthetic local batch → evidence → card → separate review → cycle → inert handoff passes and cleanup is verified.
- Negative live smoke proves no outbound connection/subprocess dispatch, external runtime write, trading/risk/paper/live action or credential output.

## Delivery gate
- Exact branch commit, test commands/results and independent reports are recorded.
- Actual GitHub PR exists with Zeus signature and `agent:zeus` label.
- For future source increments, QA Guardian merge evidence is mandatory before terminal closure; per-task waiver metadata remains the expected guard against Factory direct branch-to-base integration. The observed ALR-010-R1 `merge_no_ff_push_origin` event must be treated as reconciled audit evidence, not as delivery approval or deployment authority.
- PR-first/QA Guardian evidence must be candidate-bound: for PR #29 the candidate commit is `f61a7275048e2135b2b2729a1b9cdf8713c58866`. A review-only branch `already_ancestor` record at `5e1e4622e93d8d2fabdfe0f2176889a29afa7f7c` is not acceptable source-merge or QA Guardian evidence for that PR.
- R2m renewal evidence must be candidate-bound to the exact R2m PR head SHA on base `origin/main` `ab08b13669903a87b3d60d6c80231d23d6313782`. Obsolete project metadata pointing to PR #20 / `dad375f27568c38be771fc597b579d087f034e1d`, historical PR #29/f61a review PASS records, PR #30/c1943 merge evidence and PR #31/R2k exposure are not sufficient to change G1 docs to `reviewed: yes` or dispatch ALR-020 until canonical Factory metadata and `document_status` read back reconciled.
- R2u delivery evidence must remain documentation-only: the PR evidence records the exact R2u head/base SHA, `git diff --check`, tracked-document validation, local candidate document-status preflight with zero required blockers, and no merge/deploy/credential/external-runtime/product code change. Clearing G1 document blockers does not bypass downstream ALR task gates.

## R2v control-plane repair gate
- R2v must prove with RED then GREEN behavioral tests that stale primary checkout G1 blockers are resolved only from the verified configured base ref content, not from task branches, PR heads, or arbitrary worktrees.
- R2v must prove missing/unreadable/invalid configured base refs fail closed and that `factory_auto_integration_forbidden=true` prevents `merge_no_ff_push_origin` and `increment_integrated` Factory completion side effects.
- R2v delivery remains a Zeus-signed `agent:zeus` PR with exact branch SHA/PR readback and independent exact-SHA quality review before task closure; no direct main merge is authorized by this increment.

## R2v independent quality review — gate 804 (PASS)
- Reviewer: `quality-reviewer` profile, independent of the `codex-builder` implementation.
- Reviewed exact implementation head: `90fcb81abcebc203e16e34e36f4aec0ab1ec6a09`; code diff reviewed `50a9a29c4bb7cee39c8ffafa857ce962066e35cb..90fcb81abcebc203e16e34e36f4aec0ab1ec6a09` (2 commits: fix + PR-evidence doc).
- GitHub readback (`gh api repos/SiteOneTech/hermes-agent-original/pulls/39`): state=open, base=`main`, head_ref=`factory/zeus-alpha-research-ledger-core/inc-005-r2v-canonical-g1-status-and-no-a`, head_sha=`90fcb81abcebc203e16e34e36f4aec0ab1ec6a09`, author=`sitiouno`, labels=`["agent:zeus"]`, merged=false.
- Independent GREEN (worktree, main-checkout venv, exact head): `HERMES_PYTHON=/home/jean/Projects/hermes-agent-original/venv/bin/python3 scripts/run_tests.sh tests/hermes_cli/test_factory_control_plane_refactor.py tests/hermes_cli/test_factory_increment_integration.py` → 2 files, **254 passed, 0 failed** (9.1s, 48 workers) — matches the documented GREEN evidence exactly.
- RED: recorded in `R2V_CANONICAL_G1_STATUS_AND_NO_AUTO_MERGE_REPAIR.md` and confirmed by diff-contrast — at base `50a9a29c` the candidate-resolution path still cleared blockers, no base-ref fallback existed, and the forbidden-integration guard did not exist, so the new behavioral tests cannot pass on base code.
- Primary checkout non-mutation: `/home/jean/Projects/hermes-agent-original` HEAD remains `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1`, working tree clean; the status path performs no checkout/fetch/merge/fast-forward.
- No-external-operation: diff limited to `hermes_cli/factory_pg.py` control-plane, two test files, and five project-local docs; no deploy, credential change, direct SQL, connector/messaging action, or trading/risk/paper/live action.
- Factory DB evidence: gate `804` recorded `quality passed` for task `zeus-alpha-research-ledger-core-r2v-canonical-g1-status-and-no-auto-merg`, reviewer `quality-reviewer`.
- Non-blocking observations (no rework required): (1) no dedicated behavioral test for an entirely missing/unreadable configured base ref — the fail-closed path is covered by code inspection (`_configured_base_ref_readback` returns `accepted=false` for `repo_path_missing`/`repo_path_unreadable`/`base_ref_unavailable`) and the missing-index case is behaviorally tested; (2) `_git_file_text_at_ref` uses a 10s subprocess timeout per document (worst case ~22 docs sequential) — acceptable for a control-plane status call.
- Verdict: **PASS** — implementation head approved for R2v task closure from the quality perspective.

## R2w reviewed-frontmatter PR recovery gate
- R2w must remain documentation/review-evidence only under `factory/projects/zeus-alpha-research-ledger-core/`; no runtime/source implementation, deploy, credential, connector, messaging, direct SQL, or trading/risk/paper/live action is authorized.
- Required local evidence: `git diff --check`, scoped diff path verification, tracked-document verification, and approved Factory status CLI read-back showing zero G1 required-document blockers on configured base ref `origin/main` at `df79aac9d306c0b055fe88dbde5ebd54d9635e36`.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact candidate SHA named in the PR body, and independent exact-SHA `quality-reviewer` verification before task closure. This worker must not merge the PR.

## R2ah current-origin reviewed-marker/index repair gate
- R2ah must remain documentation/review-evidence only under `factory/projects/zeus-alpha-research-ledger-core/`; no runtime/source implementation, deploy, credential, connector, messaging, direct SQL, or trading/risk/paper/live action is authorized.
- Required local evidence: fresh worktree identity captured before edits, `git diff --check`, scoped diff path verification, tracked-document verification, and approved Factory status CLI read-back showing all 14 G1 required documents exist, are committed, indexed, validated, reviewed, and non-blocking on configured base ref `origin/main` at `1b6bc0f65d3ad49845d20e056203e3b3702ac2a7`.
- Delivery evidence must be PR-first: actual non-draft GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact candidate SHA named in the PR body, and independent exact-SHA `quality-reviewer` verification before task closure. This worker must not merge the PR.

## R2ao current-origin G1 control-plane projection repair gate
- R2ao must prove with RED then GREEN behavioral tests that structured `unvalidated_required_docs` blockers resolve from current required-document rows when all rows are non-blocking, while remaining fail-closed when any current row still blocks.
- R2ao must prove reconciliation removes obsolete `metadata.g1_documentation_checkout` from the persisted project projection only after the current finding set no longer includes `unvalidated_required_docs` and current G1 rows are clean.
- Required local evidence: canonical Factory status reproduction from the assigned worktree, `git diff --check`, targeted RED/GREEN test output, full relevant Factory control-plane and increment integration test-file runs, Zeus-signed `agent:zeus` PR, exact candidate SHA in the PR body, and independent security review request. This worker must not merge, deploy, direct-SQL mutate, or touch the primary checkout.

## R2c2 autonomous canonical G1 document-status repair gate
- R2c2 must remain documentation/review-evidence only under `factory/projects/zeus-alpha-research-ledger-core/`; no runtime/source implementation, deploy, credential, connector, messaging, direct SQL, or trading/risk/paper/live action is authorized.
- Required local evidence: fresh worktree identity captured before edits, `git diff --check`, scoped diff path verification, tracked-document verification, and approved Factory status CLI read-back showing all 14 G1 required documents exist, are committed, indexed, validated, reviewed, and non-blocking on configured base ref `origin/main` at `dbde1790f8d45f111bc69b3491a1862eafb29fa2`.
- Delivery evidence must be PR-first: actual non-draft GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact candidate SHA named in the PR body/Factory evidence, and independent exact-SHA `quality-reviewer` verification before task closure. This worker must not self-approve or merge the PR.

## R2c4 canonical source-selection repair gate
- R2c4 is limited to Factory control-plane/document-status source selection plus project-local evidence docs; no product runtime, deploy, credential, connector, messaging, direct SQL, or trading/risk/paper/live action is authorized.
- Required RED evidence: a stale primary checkout with ready G1 markers must not clear blockers when configured `origin/main` has pending markers; the test must fail before the resolver repair.
- Required GREEN evidence: focused `scripts/run_tests.sh` coverage must prove stale primary checkout identity is rejected, configured `origin/main` is used deterministically, invalid/missing configured base fails closed, candidate PR/worktree sources remain non-canonical, and G0/G1 gates continue to pass.
- Delivery evidence must be PR-first: actual non-draft GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base/head/source SHAs named in the PR body/Factory evidence, zero required G1 blockers at the resolved configured source, and independent exact-SHA `quality-reviewer` verification before task closure. This worker must not self-approve or merge the PR.

## R2c5 independent current-base G1 review gate (gate 832 — PASS)
- Reviewer: `quality-reviewer` profile; independent of the codex-builder documentation/control-plane rounds and performed at the exact current base, not a self-approval.
- Reviewed candidate: project-local G1 documentation pack at exact base `origin/main` `91aa62b11f02f69d88f7d8c18c30033edb4b7355` in the assigned isolated worktree `factory/zeus-alpha-research-ledger-core/inc-001-r2c5-independent-current-base-g1` (branch `factory/zeus-alpha-research-ledger-core/inc-001-r2c5-independent-current-base-g1`).
- All 14 Factory-required G1 documents verified: `exists=true`, `committed=true`, `indexed=true`, `validated=true`, `reviewed=true`, `blocking=false` at the configured base source; canonical readback evidence in `/tmp/r2c5_readback_new_resolver.json` lines 17420–17770 (`readiness_source=configured_base_ref`, `base_commit=91aa62b11f02f69d88f7d8c18c30033edb4b7355`, `configured_base_ref_accepted=true`, `primary_checkout_accepted=false`).
- Content review: every required doc internally consistent with `DOCUMENTATION_INDEX.md`, `DATABASE_AND_RUNTIME_CONTRACT.md`, G0, traceability and repair records; no doc claims external runtime, trading/risk/paper/live, deployment, credential, connector or messaging authority; PR-first/QA-Guardian and no-auto-integration contracts consistently documented.
- Live runtime mismatch (exact output in `/home/jean/.hermes/profiles/quality-reviewer/cache/terminal-output/out-1786901573-3764810-ead0.log` lines 17420–17729: 10 required G1 rows `reviewed=false, blocking=true` from the stale primary checkout running the pre-R2v resolver) is documented in `R2C5_INDEPENDENT_CURRENT_BASE_G1_REVIEW.md` and routed as bounded technical rework — a Factory runtime catch-up of the primary checkout to `origin/main`; not resolvable by this documentation-only increment.
- Factory DB evidence: gate `832` recorded `quality passed` for project `zeus-alpha-research-ledger-core`, reviewer `quality-reviewer`.
- Delivery evidence must be PR-first: actual non-draft GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base `91aa62b11` and final head SHA named in the PR body/evidence, and independent exact-SHA review (task reviewer `solution-architect`) before task closure. The R2c5 PR is #51 `https://github.com/SiteOneTech/hermes-agent-original/pull/51`. This worker must not self-approve or merge the PR.
- Verdict: **PASS** — the required G1 documentation pack at exact base `91aa62b11` is approved from the quality perspective; the canonical repair is documentation/index/evidence only.

## R2c6 bounded current-origin G1 resolver/readback recovery gate
- R2c6 is limited to Factory G1 `document_status` resolver/readback behavior plus project-local evidence documentation; no primary-checkout mutation, merge, deploy, credential, connector, messaging, direct SQL, external runtime, product ledger, trading/risk/paper/live action, or promotion capability is authorized.
- Required divergence evidence: primary checkout `/home/jean/Projects/hermes-agent-original` remains `main...origin/main [ahead 3, behind 1370]`, HEAD `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1`, `origin/main` `40a188b23a384901f983e4d959d3ebbecf50b318`, merge-base `c846ccfbd844c2f8810a26776505ec44a2341914`; the assigned worktree starts at current `origin/main` with ahead/behind `0	0`.
- Required RED/GREEN: tests must prove an accepted reviewed candidate tied to the exact configured base can be read without moving the primary checkout, and unavailable, dirty, malformed, untracked, or unreviewed candidates remain blocking. Recorded evidence: targeted RED failed 6 selected tests before implementation; GREEN passed 6 selected tests, then 148 control-plane tests, then 261 related tests across `test_factory_control_plane_refactor.py` and `test_factory_increment_integration.py`.
- Required Factory CLI readback: approved CLI status (`/home/jean/Projects/hermes-agent-original/venv/bin/python3 -m hermes_cli.main factory status zeus-alpha-research-ledger-core --json`) exits 0 and records configured-base readback at `base_commit=40a188b23a384901f983e4d959d3ebbecf50b318`, `primary_checkout_accepted=false`, with exact G1 rows in `/home/jean/.hermes/profiles/codex-builder/cache/terminal-output/out-1786906211-500656-f450.log` lines 17822–18172.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact candidate SHA named in the PR body/Factory gate, and independent exact-SHA review before task closure. This worker must not self-approve, merge, or mutate the primary checkout.

## R2am stale-primary Factory tick source-resolution gate
- R2am is limited to Factory tick source selection for CLI/dashboard project tick and resume paths plus project-local evidence docs; no product runtime, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, trading/risk/paper/live action, or promotion capability is authorized.
- Required RED evidence: `hermes_cli.factory._run_orchestrator_script()` and the dashboard Factory tick route must fail tests while they can still execute the profile wrapper that hardcodes `/home/jean/Projects/hermes-agent-original/scripts/factory/factory_orchestrator_tick.py` instead of the running worktree source.
- Required GREEN evidence: focused tests must prove the CLI helper executes the tick script from the running `hermes_cli.factory` source tree, sets matching `cwd` and `PYTHONPATH`, ignores a stale profile wrapper, and fails closed for unavailable or malformed provenance; dashboard tick/resume must route through the same helper.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact candidate SHA named in the PR body/Factory evidence, and independent exact-SHA quality review before any G1 recovery or implementation dispatch relies on the repair. This worker must not self-approve, merge, deploy, run direct SQL, or mutate the primary checkout.

## G1 document-status technical recovery gate
- This recovery is limited to project-local Markdown provenance under `factory/projects/zeus-alpha-research-ledger-core/`; no product code, Factory runtime code, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, trading/risk/paper/live action, or promotion capability is authorized.
- Required current-origin evidence: assigned worktree `HEAD`, `origin/main`, and merge-base all equal `139df9ae49137bb4b16152550d53d385310de3b6`; Factory CLI status output exits 0 and shows all 14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, `configured_base_ref_accepted=true`, and stale primary rejected.
- Required mismatch evidence: project-local artifact records stale persisted `metadata.reconciliation_anomalies`, obsolete PR #20 checkout metadata, and historical failed gate snapshots separately from current dynamic document rows; any remaining dispatch block must be routed as bounded control-plane repair, not solved by direct SQL or primary checkout mutation.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base/head/status-output provenance in the PR body, and independent quality review before downstream dispatch relies on this recovery. This worker must not self-approve or merge.

## R2aj isolated current-base G1 documentation evidence recovery gate
- R2aj is limited to project-local Markdown provenance under `factory/projects/zeus-alpha-research-ledger-core/`; no product code, Factory runtime code, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, trading/risk/paper/live action, or promotion capability is authorized.
- Required current-base evidence: assigned worktree `HEAD`, `origin/main`, and merge-base all equal `bf422968f9ea73d70d4ac1e8b8bae4af644ce079`; Factory CLI status output `/home/jean/.hermes/profiles/codex-builder/cache/terminal-output/out-1786939834-4163626-ca90.log` exits 0 and shows all 14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, `configured_base_ref_accepted=true`, and stale primary rejected at lines 19860–20210.
- Required PR-evidence guard: PR #44 is not current-base evidence because GitHub readback shows it is open on branch `factory/zeus-alpha-research-ledger-core/inc-019-r2ae-bounded-canonical-g1-valida` at head `768444e33ac64bf238e64c1df4c49fe2020b51a8`. R2aj must produce a fresh Zeus-signed `agent:zeus` PR from branch `factory/zeus-alpha-research-ledger-core/inc-017-r2aj-isolated-current-base-g1-do` and bind independent review to its exact head SHA.
- Required mismatch evidence: project-local artifact records stale persisted `reconciliation_anomalies=["unvalidated_required_docs"]`, obsolete PR #20 checkout metadata, and dispatch preflight `missing_or_unindexed_docs` separately from current dynamic document rows; any remaining dispatch block must be routed as bounded control-plane repair, not solved by direct SQL or primary checkout mutation.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base/head/status-output provenance in the PR body, and independent quality review before downstream dispatch relies on this recovery. This worker must not self-approve or merge.

## R2ap current-origin G1 document validation recovery gate
- R2ap is limited to project-local Markdown provenance under `factory/projects/zeus-alpha-research-ledger-core/`; no product code, Factory runtime code, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, trading/risk/paper/live action, or promotion capability is authorized.
- Required current-origin evidence: assigned worktree `HEAD`, `origin/main`, and merge-base all equal `3e32da02c218e06a69b851641b2d454113654378`; Factory CLI status output `/home/jean/.hermes/profiles/codex-builder/cache/terminal-output/out-1786948307-1932752-1750.log` exits 0 and shows all 14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, `configured_base_ref_accepted=true`, and stale primary rejected at lines `20070`–`20420`.
- Required RED/GREEN documentation validator: `python3 /tmp/validate_r2ap_docs.py` must fail before repair because the R2ap artifact/index/gate/tracker/task-graph markers are absent, then pass after repair. `git diff --check` must exit 0.
- Required resolve-state readback: the same Factory status output must show stale failure evidence separately from current rows (`unvalidated_required_docs` / `missing_or_unindexed_docs` in historical event/gate records) and active metadata `reconciliation_anomalies=[]`, `reconciliation_projection_source=current_document_status`, `reconciliation_required=false`, with the old `unvalidated_required_docs` retained only under audit-only `stale_reconciliation_projection`.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base/head/status-output provenance in the PR body, explicit no-external-execution statement, and independent exact-SHA quality review before downstream dispatch relies on this recovery. This worker must not self-approve, merge, deploy, direct-SQL mutate, or execute external runtimes.

## R2at current-origin G1 documentation validation gate
- R2at is limited to project-local Markdown provenance under `factory/projects/zeus-alpha-research-ledger-core/`; no product code, Factory runtime code, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, trading/risk/paper/live action, promotion capability, or no-auto-merge bypass is authorized.
- Required current-origin evidence: assigned worktree `HEAD`, `origin/main`, and merge-base all equal `a41acdc4820b92a31b7d42d9a9c28e95b875a3d1`; Factory CLI status output `/home/jean/.hermes/profiles/claude-builder/cache/terminal-output/out-1786955910-3629985-7910.log` exits 0 and shows all 14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, `configured_base_ref_accepted=true`, and stale primary rejected at lines `20184`–`20534`.
- Required RED/GREEN documentation validator: `python3 /tmp/validate_r2at_docs.py` must fail before repair because the R2at artifact/index/gate/tracker/task-graph/G1-review/G0 markers are absent, then pass after repair. `git diff --check` must exit 0.
- Required stale-condition readback: the same Factory status output must identify stale events `194478`/`194477` (`unvalidated_required_docs`) and dispatch-preflight event `194474` (`missing_or_unindexed_docs`) separately from current rows, while active metadata reports `reconciliation_anomalies=[]`, `reconciliation_projection_source=current_document_status`, and `reconciliation_required=false`, with the old `unvalidated_required_docs` retained only under audit-only `stale_reconciliation_projection`.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base/head/status-output provenance in the PR body, explicit no-auto-merge and no-external-execution statements, and independent exact-SHA quality review before downstream dispatch relies on this recovery. This worker must not self-approve, merge, deploy, direct-SQL mutate, mutate the primary checkout, or execute external runtimes.

## R2au current-origin G1 document-status projection repair gate
- R2au is limited to Factory document-status projection logic plus project-local evidence docs; no product ledger implementation, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, base-branch merge, trading/risk/paper/live action, promotion capability, or no-auto-merge bypass is authorized.
- Required RED evidence: `scripts/run_tests.sh tests/hermes_cli/test_factory_control_plane_refactor.py -k status_projection_uses_origin_base_not_stale_head_or_task_metadata -v --tb=short` must fail before the repair because stale status metadata still exposes `unvalidated_required_docs` even when configured `origin/main` G1 rows are non-blocking.
- Required GREEN evidence: targeted status-projection tests must pass after repair; relevant full-file control-plane coverage must pass before PR handoff.
- Required current-origin readback: `origin/main` must equal `2b53ee0f14491ff43da7683d475654a03af5d678`, contain R2at commit `d4ac6d89994adf823bb50b79afe5a39fd204fdfd`, and Factory status output `/home/jean/.hermes/profiles/codex-builder/cache/terminal-output/out-1786959574-145359-db10.log` must show all 14 required G1 rows non-blocking at lines `20255`–`20604`, with active metadata lines `20808`–`20844` no longer carrying stale required-doc projection metadata.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base/head/status-output provenance, explicit no direct SQL/no primary mutation/no external runtime execution statement, and independent exact-SHA review before downstream dispatch relies on this repair. This worker must not self-approve or merge.

## R2av current-origin G1 status projection verification gate
- R2av is limited to isolated current-origin Factory status verification and project-local evidence docs; no product ledger implementation, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, base-branch merge, trading/risk/paper/live action, promotion capability, or no-auto-merge bypass is authorized.
- Required current-origin readback: assigned worktree `HEAD`, `origin/main`, merge-base, and remote `refs/heads/main` must all equal `af9fa27eaaaa52ef173f1578fb7f572ce52cebc6`; Factory CLI status output `/home/jean/.hermes/profiles/codex-builder/cache/terminal-output/out-1786962029-820188-d410.log` must show all 14 active G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, and stale primary rejected at lines `20270`–`20626`.
- Required mismatch explanation: `R2AV_CURRENT_ORIGIN_G1_STATUS_PROJECTION_RECOVERY.md` must identify stale primary/runtime and historical gate/event rows as the source of old `reviewed=false` projections, and must not treat those rows as current dispatch readiness.
- Required GREEN evidence: the R2au status-projection tests must pass from the R2av current-origin worktree; R2av records the existing R2au RED as the focused failing reproduction for this bug class and adds no second code repair when the current test is already green.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base/head/status-output provenance, explicit no direct SQL/no primary mutation/no external runtime execution statement, and independent exact-SHA quality review plus QA Guardian evidence before downstream dispatch relies on this verification. This worker must not self-approve or merge.

## R2bb current-base G1 status projection / PR #63 evidence recovery gate
- R2bb is limited to Factory status-source provenance readback plus project-local evidence docs; no product ledger implementation, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, base-branch merge, trading/risk/paper/live action, promotion capability, or no-auto-merge bypass is authorized.
- Required current-base readback: assigned worktree `HEAD`, `origin/main`, merge-base, and remote `refs/heads/main` must all equal `b05afe59c88cfa7f7dbec0117603b2f052267ce0`, the PR #63/R2aw merge commit; PR #63 readback must show non-draft merged `agent:zeus` PR with base `52df8d7c6599e3ec2ec4559e0139ffd91ec74011` and head `dcd9c74f252d288269d746ab59079a0221de7a46`.
- Required RED/GREEN evidence: focused status delegation test must fail before repair because delegated JSON status lacks deterministic `factory_cli_source_root` provenance, then pass after repair with `factory_cli_source_root`, `factory_status_source_root`, `factory_status_delegated`, and delegated-from source root present.
- Required Factory status readback: approved CLI status from the assigned worktree must expose the worktree source root, report all 14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, base `b05afe59c88cfa7f7dbec0117603b2f052267ce0`, `reconciliation_anomalies=[]`, and stale primary `4eb87e4cd48105af05fe974cf1d493f0e1b57ae1` rejected.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base/head/status-output provenance, explicit no direct SQL/no primary mutation/no external runtime execution statement, and independent exact-SHA quality review before downstream dispatch relies on this recovery. This worker must not self-approve or merge.

## R2BJ bounded canonical G1 documentation/index recovery gate
- R2BJ is limited to current-base project-local documentation/index/evidence under `factory/projects/zeus-alpha-research-ledger-core/`; no product ledger implementation, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, base-branch merge, trading/risk/paper/live action, promotion capability, or no-auto-merge bypass is authorized.
- Required current-base readback: assigned worktree `HEAD`, `origin/main`, and merge-base must all equal `b503ba3b57fd606956d0ebf925c83eda253bdcc5`; Factory status JSON from the assigned worktree must expose worktree source-root provenance, `factory_status_delegated=false`, `reconciliation_anomalies=[]`, `reconciliation_projection_source=current_document_status`, `reconciliation_required=false`, `notion_required=false`, and all 14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, with stale primary checkout rejected.
- Required predicate evidence: `R2BJ_BOUNDED_CANONICAL_G1_DOCUMENTATION_INDEX_RECOVERY.md` must name the exact docs-first predicate that maps `docs_ready=false` to `missing_or_unindexed_docs`, and must separate stale event/gate projections (`195559`, `195563`, `195562`, `195560`, gate `884`/PR #44) from current configured-base row truth.
- Required validation: `git diff --check`, tracked-file verification for changed docs and the new artifact, canonical Factory status readback, and focused Factory status/projection regression tests must pass before PR handoff.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, immutable final commit SHA, current branch provenance, validation output, explicit no-merge/no-direct-SQL/no primary mutation/no external runtime execution statement, and independent exact-SHA quality review before downstream dispatch relies on this recovery. This worker must not self-approve or merge.

## R2cm review-state provenance repair gate
- R2cm is limited to current-base project-local documentation/provenance under `factory/projects/zeus-alpha-research-ledger-core/`; no product ledger implementation, Factory runtime change, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, base-branch merge, trading/risk/paper/live action, promotion capability, or no-auto-merge bypass is authorized.
- Required current-base identity: assigned worktree `HEAD`, `origin/main`, and merge-base must all equal `0ecd9019ba8ec111aaead60a911c9accd854f731`; branch/worktree must be `factory/zeus-alpha-research-ledger-core/inc-001-r2cm-repair-g1-review-state-prov` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-001-r2cm-repair-g1-review-state-prov`.
- Required canonical readback: `/home/jean/Projects/hermes-agent-original/venv/bin/hermes factory status zeus-alpha-research-ledger-core --json` captured `/tmp/r2cm-status-before.json` and must list exactly these ten G1 blockers with missing `reviewed`: `FACTORY_INTAKE.md`, `REQUIREMENTS_ANALYSIS.md`, `PATTERN_ANALYSIS.md`, `ASSUMPTIONS_AND_OPEN_QUESTIONS.md`, `PRD.md`, `ADRS.md`, `METHODOLOGY_PLAN.md`, `TECHNICAL_BLUEPRINT.md`, `TASK_GRAPH.md`, `SECURITY_GATES.md`.
- Required provenance correction: R2cl task/run completion or terminal auto-finalization is not independent review evidence because the R2cl quality-review path exhausted on MiniMax HTTP 429. Existing G1 `reviewed: yes` markers may remain green only because their real provenance is PR #36 / Factory gate `794` (source gate `790` / SHA `2476e978c545e24b18ee48844b24eb8c58245ab4`), not R2cl.
- Required validation: `git diff --check`, tracked-file verification for changed docs and the new artifact, canonical Factory readback, diagnostic current-base readback, and focused resolver/document-status regression tests through `scripts/run_tests.sh` must pass before PR handoff.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, immutable final commit SHA, current branch provenance, validation output, explicit no-merge/no-direct-SQL/no primary mutation/no external runtime execution statement, and independent exact-SHA quality review before downstream dispatch relies on this recovery. If the independent review provider rate-limits again, keep this gate pending/failed rather than green. This worker must not self-approve or merge.

## R2cn bounded canonical G1 docs gate / PR-provenance repair gate
- R2cn is limited to current-base project-local documentation/provenance under `factory/projects/zeus-alpha-research-ledger-core/`; no product ledger implementation, Factory runtime change, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, base-branch merge, trading/risk/paper/live action, promotion capability, or no-auto-merge bypass is authorized.
- Required current-base identity: assigned worktree `HEAD`, `origin/main`, merge-base, and remote `refs/heads/main` must all equal `fa24950a228f28d5106ee2125d42045e872f9504`; branch/worktree must be `factory/zeus-alpha-research-ledger-core/inc-015-r2cn-bounded-canonical-g1-docs-g` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-015-r2cn-bounded-canonical-g1-docs-g`.
- Required canonical readbacks: `/tmp/r2cn-status-before.json` and `/tmp/r2cn-status-after-resolve.json` must report 14/14 required G1 rows non-blocking from `readiness_source=configured_base_ref`, base `fa24950a228f28d5106ee2125d42045e872f9504`, with stale primary rejected; `/tmp/r2cn-resolve-state.json` must show stale `unvalidated_required_docs` anomalies cleared for historical tasks or document any concrete remaining technical defect.
- Required validation: `git diff --check`, tracked-file verification for changed docs and the new artifact, canonical Factory status/resolve-state readbacks, and focused resolver/document-status regression tests through `scripts/run_tests.sh` must pass before PR handoff.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, immutable final commit SHA, current branch provenance, validation output, explicit no-merge/no-direct-SQL/no primary mutation/no external runtime execution statement, and independent exact-SHA quality review before downstream dispatch relies on this recovery. This worker must not self-approve or merge.

## R2ai-R2 non-destructive current-origin G1 documentation recovery gate
- R2ai-R2 is limited to current-origin project-local documentation/evidence under `factory/projects/zeus-alpha-research-ledger-core/`; no product ledger implementation, Factory runtime change, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, base-branch merge, force-push/reset/rewrite of any existing remote ref, trading/risk/paper/live action, promotion capability, or no-auto-merge bypass is authorized.
- Required current-base identity: assigned worktree `HEAD`, `origin/main`, merge-base, and remote `refs/heads/main` must all equal `6c07c2fee59679a5b0063e635f0332895dbb3ec5`; branch/worktree must be `factory/zeus-alpha-research-ledger-core/inc-017-r2ai-r2-non-destructive-current` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-017-r2ai-r2-non-destructive-current`; the assigned branch must not pre-exist on origin (non-destructive fresh-branch push, no force).
- Required canonical readback: `/tmp/r2ai-r2-status-before.json` (assigned-worktree cwd, exit 0) must report 14/14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false` from `readiness_source=configured_base_ref`, base `6c07c2fee59679a5b0063e635f0332895dbb3ec5`, stale primary `4eb87e4cd4…` rejected as `primary_checkout_not_configured_base`, and active project metadata `reconciliation_anomalies=[]` from `current_document_status`. Historical readbacks `/tmp/r2ai_rework_factory_status.json` (primary-cwd, 10 stale-blocking rows) and `/tmp/r2ai_rework_status_worktree_cwd.json` (14/14 clean) are preserved as R2ai-R1 evidence.
- Required record: remaining stale task-level `unvalidated_required_docs` metadata on the blocked R2ai task must be named as the exact bounded follow-up Factory task `zeus-alpha-research-ledger-core-r2ai-r2-canonical-active-metadata-anomaly-repair`, not silently cleared, self-approved, or SQL-mutated.
- Required validation: `git diff --check`, tracked-file verification for changed docs and the new artifact, and the canonical Factory CLI readback above must pass before PR handoff.
- Delivery evidence must be PR-first: actual non-draft GitHub PR against `main`, Zeus signature, `agent:zeus` label, immutable final commit SHA, exact base/head/ancestry evidence, validation output, explicit no-force-push/no-merge/no-direct-SQL/no primary mutation/no external runtime execution statement, and independent exact-SHA quality review before downstream dispatch relies on this recovery. This worker must not self-approve or merge.

## R2ap PR #72 residual G1 task metadata reconciliation gate
- R2ap-PR72 is limited to project-local documentation/evidence under `factory/projects/zeus-alpha-research-ledger-core/`; no product/runtime code, Factory runtime change, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, merge, rebase, force-push/ref rewrite, trading/risk/paper/live action, or no-auto-merge bypass is authorized.
- Required current-base readback: assigned worktree `HEAD` and `origin/main` must equal `c31e937111bba64e478d3c319e896774bf09e40e`; canonical Factory status `/tmp/r2ap-pr72-status-before.json` must report 14/14 required G1 rows non-blocking from `readiness_source=configured_base_ref`, stale primary rejected, and active metadata `reconciliation_anomalies=[]` from `current_document_status`.
- Required PR evidence: GitHub PR #72 must read back as non-draft, `agent:zeus`, base `main` / `c31e937111bba64e478d3c319e896774bf09e40e`, head `3311b82ee1a29043039003e94582509bb8b89895`, clean, and docs-only under `factory/projects/zeus-alpha-research-ledger-core/`, with Factory gates `906` and `907` already passed for the PR #72 repair task.
- Required residual-source finding: the artifact must name the residual source as stale blocked R2ai/R2ae task metadata (`blocker_source=structured_reconciliation_metadata`, `reconciliation_anomaly/resolved_anomaly=unvalidated_required_docs`) and distinguish it from current configured-base document rows.
- Required fail-closed closure rule: old R2ai/R2ae tasks may be closed/superseded only through an explicitly authorized canonical closure command. If the run is limited to `factory status` and `factory gate record`, the artifact must state that `factory gate record` cannot mutate task status and leave those rows fail-closed.
- Required validation: `git diff --check`, tracked-file verification for changed docs and the new artifact, canonical Factory status readback, and read-only PR #72 readback must pass before PR handoff. Independent exact-SHA quality review remains required; this worker must not self-approve or merge.

## R2ax current-origin Factory CLI G1 recovery dispatch gate
- R2ax is limited to Factory CLI control-plane source selection/provenance plus project-local evidence docs; no ALR product/runtime code, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, merge, force-push/ref rewrite, trading/risk/paper/live action, or no-auto-merge bypass is authorized.
- Required current-base identity: assigned worktree `HEAD`, `origin/main`, and merge-base must all equal `e7e216272ea64a83351ae38f27688ecda47cbbbf`; branch/worktree must be `factory/zeus-alpha-research-ledger-core/inc-018-r2ax-current-origin-factory-cli` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-018-r2ax-current-origin-factory-cli`.
- Required RED/GREEN evidence: `scripts/run_tests.sh tests/hermes_cli/test_factory_orchestrator_tick.py -k 'resolve_state_prefers_isolated_cwd_source_over_stale_running_module or resolve_state_delegation_failure_does_not_fall_back_to_stale_backend' -v --tb=short` must fail before the repair because `resolve-state` uses stale backend code, then pass after the repair with current-origin delegated source root and fail-closed delegated-error behavior.
- Required broader validation: full `tests/hermes_cli/test_factory_orchestrator_tick.py`, focused current-origin document-status tests in `tests/hermes_cli/test_factory_control_plane_refactor.py`, and `git diff --check` must pass before PR handoff.
- Required canonical readback: approved `factory status` CLI from the assigned worktree must expose source roots equal to the worktree, `reconciliation_anomalies=[]`, `reconciliation_projection_source=current_document_status`, 14/14 required G1 rows non-blocking from `readiness_source=configured_base_ref`, base `e7e216272ea64a83351ae38f27688ecda47cbbbf`, and stale primary rejected. Live `resolve-state`/`tick` execution is not required when the run's DB-write allowlist permits only `factory status` and `factory gate record`; behavioral tests must cover those source paths instead.
- Delivery evidence must be PR-first: actual GitHub PR against `main`, Zeus signature, `agent:zeus` label, immutable final commit SHA, current branch provenance, validation output, explicit no-merge/no-direct-SQL/no primary mutation/no external runtime/no ALR-020 statement, and independent exact-SHA review before downstream dispatch relies on this recovery. This worker must not self-approve or merge.

## R2az non-destructive current-base G1 evidence recovery gate
- R2az is limited to project-local documentation/evidence under `factory/projects/zeus-alpha-research-ledger-core/`; no runtime/product code, Factory runtime code, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, task-status mutation, merge, rebase, force-push/ref rewrite, stale PR mutation, trading/risk/paper/live action, or no-auto-merge bypass is authorized.
- Required current-base identity: assigned worktree `HEAD`, `origin/main`, remote `refs/heads/main`, and merge-base must all equal `3b7bc91f2ee1ef603bb512d147c692568c1b465f`; branch/worktree must be `factory/zeus-alpha-research-ledger-core/inc-017-r2az-non-destructive-current-bas` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-017-r2az-non-destructive-current-bas`, and the assigned remote branch must be absent before the normal first push.
- Required canonical readback: approved `factory status` CLI from the assigned worktree must report Agent Core Postgres, source roots equal to the worktree, `factory_status_delegated=false`, 14/14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, and stale primary rejected. The exact remaining condition must be recorded as stale event/task-level projection (`project_reconciled` events `197638`/`197637`/`197635`, dispatch-preflight event `197634`, and old blocked R2ai/R2ae structured metadata), not a current document-content blocker.
- Required validation before PR handoff: `git diff --check`, changed-path confinement to `factory/projects/zeus-alpha-research-ledger-core/`, `git ls-files --error-unmatch` for the R2az artifact and edited docs, canonical Factory status readback, and PR readback after push.
- Delivery evidence must be PR-first: actual non-draft GitHub PR against `main`, Zeus Signed-off-by commit, `agent:zeus` label, immutable final candidate SHA, base ancestry, G1 validation/status readback, gate readback, explicit no-merge/no-direct-SQL/no-primary-mutation/no-force-push/no external runtime/no ALR-020 statement, and independent exact-SHA review before downstream dispatch relies on this recovery. This worker must not self-approve or merge.

## R2ba current-base G1 independent review and review-state repair gate (gates 915/916 — PASS)

- Reviewer: `quality-reviewer` profile; independent of prior builder/control-plane rounds and performed at the exact current base `origin/main` `756ac62a4c69278216b2b7e66b34e6f11ad54c29` (R2az PR #76 merge), not a self-approval of a historical or different-SHA candidate.
- Reviewed candidate: project-local G1 documentation pack at exact base `756ac62a4c…` in the assigned isolated worktree `factory/zeus-alpha-research-ledger-core/inc-001-r2ba-current-base-g1-independent` (branch `factory/zeus-alpha-research-ledger-core/inc-001-r2ba-current-base-g1-independent`).
- All 14 Factory-required G1 documents verified: `exists=true`, `committed=true`, `indexed=true`, `validated=true`, `reviewed=true`, `blocking=false` at the configured base source; canonical readback evidence in `/tmp/r2ba-status-before.json` (`readiness_source=configured_base_ref`, `base_commit=756ac62a4c69278216b2b7e66b34e6f11ad54c29`, `configured_base_ref_accepted=true`, `primary_checkout_accepted=false`, source roots equal to the worktree, `factory_status_delegated=false`, active `reconciliation_anomalies=[]`).
- Content review: every required doc internally consistent with `DOCUMENTATION_INDEX.md`, `DATABASE_AND_RUNTIME_CONTRACT.md`, G0, traceability and repair records; no doc claims external runtime, trading/risk/paper/live, deployment, credential, connector or messaging authority; PR-first/QA-Guardian and no-auto-integration contracts consistently documented.
- Exact blocker identification: the ten-document `reviewed=false` projection named in the task description (`FACTORY_INTAKE.md`, `REQUIREMENTS_ANALYSIS.md`, `PATTERN_ANALYSIS.md`, `ASSUMPTIONS_AND_OPEN_QUESTIONS.md`, `PRD.md`, `ADRS.md`, `METHODOLOGY_PLAN.md`, `TECHNICAL_BLUEPRINT.md`, `TASK_GRAPH.md`, `SECURITY_GATES.md`) is stale-primary/historical evidence — the primary checkout `/home/jean/Projects/hermes-agent-original` HEAD `4eb87e4cd4…` runs the pre-R2v resolver over `reviewed: pending` working-tree files and never reaches the configured base source — not current configured-base state; it is routed as bounded technical rework (Factory runtime/primary-checkout catch-up), not a documentation-content blocker.
- Factory DB evidence: gates `915` (`quality passed`) and `916` (`security passed`) recorded for project `zeus-alpha-research-ledger-core`, task `zeus-alpha-research-ledger-core-r2ba-current-base-g1-independent-review-`, reviewer `quality-reviewer`, at exact candidate SHA `756ac62a4c69278216b2b7e66b34e6f11ad54c29`.
- Review-state rule: required-document `reviewed=yes` frontmatter markers remain bound to PR #36 / gate `794` (source gate `790` / PR #34 SHA `2476e978c5…`) and are not modified; the fresh R2ba exact-SHA assessment is recorded here and in Factory gates as current-base review evidence, preserving fail-closed semantics elsewhere.
- Supersession rule: obsolete blocked R2ai/R2ae/R2ac rows stay fail-closed because the run's DB-write allowlist covers only `factory status` and `factory gate record`; evidence-backed supersession, if authorized later, must go through the canonical Factory CLI without deleting audit history.
- Delivery evidence must be PR-first: actual non-draft GitHub PR against `main`, Zeus signature, `agent:zeus` label, exact base `756ac62a4c` and final head SHA named in the PR body/evidence, and independent exact-SHA review before task closure. This worker must not self-approve or merge the PR.

## R2bl non-destructive canonical G1 evidence repair gate

- R2bl is limited to project-local documentation/evidence and the project-local validator under `factory/projects/zeus-alpha-research-ledger-core/`; no runtime/product code, Factory runtime code, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, task-status mutation, merge, rebase, force-push/ref rewrite, stale PR mutation, trading/risk/paper/live action, or no-auto-merge bypass is authorized.
- Required current-base identity: assigned worktree `HEAD`, `origin/main`, and merge-base must all equal `faddaf5afb4c1754e03d8c97dd6706353b5b0865`; branch/worktree must be `factory/zeus-alpha-research-ledger-core/inc-001-r2bl-non-destructive-canonical-g` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-001-r2bl-non-destructive-canonical-g`, and the assigned remote branch must be absent before normal first push.
- Required canonical readback: approved `factory status` CLI from the assigned worktree must report Agent Core Postgres, source roots equal to the worktree, `factory_status_delegated=false`, 14/14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, and stale primary rejected. The exact residual source must be recorded as stale structured R2ai/R2ae task metadata plus historical event projection, not current document content.
- Required deterministic validation: `python3 factory/projects/zeus-alpha-research-ledger-core/validate_r2bl_g1_evidence.py --status-json <status-json> --expected-base faddaf5afb4c1754e03d8c97dd6706353b5b0865 --expected-head <final-pr-head-sha> --expected-pr https://github.com/SiteOneTech/hermes-agent-original/pull/78 --expected-quality-gate <quality-gate-id> --expected-security-gate <security-gate-id>` must fail before the R2bl artifact/index/gate/tracker evidence exists and pass only after a fresh PR and independent exact-SHA quality/security gate records are visible in canonical status.
- Delivery evidence must be PR-first: actual non-draft GitHub PR `https://github.com/SiteOneTech/hermes-agent-original/pull/78` against `main`, Zeus Signed-off-by commit, `agent:zeus` label, immutable final candidate SHA in the PR body/Factory gates, base ancestry, G1 validation/status readback, explicit no-merge/no-direct-SQL/no-primary-mutation/no-force-push/no external runtime/no ALR-020 statement, and independent exact-SHA quality + security review before downstream dispatch relies on this repair. This worker must not self-approve or merge.

## R2bm canonical G1 docs gate source-root recovery gate (gates 925/926 — PASS)

- R2bm source-root repair reached `origin/main` as merge commit `9ebaa9e7b44c61bb871ca4da0a838c52e62666b2` after PR #79 head `06051d990821bc7127004313ca3458e0394832d8`; canonical Factory status and resolve-state readbacks remained clean (`14/14` required G1 rows non-blocking, `reconciliation_anomalies=[]`, `supervisor.health=green`).
- Factory DB evidence: gate `925` recorded independent exact-SHA quality PASS by `quality-reviewer`; gate `926` recorded implementation evidence by `claude-builder`. This is current-base source-root control evidence only and does not authorize ALR-020/product dispatch.

## R2bn canonical G1 review-state source-root repair gate (gate 929 — PASS)

- R2bn is limited to project-local documentation/evidence and the project-local validator under `factory/projects/zeus-alpha-research-ledger-core/`; no runtime/product code, Factory runtime code, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, task-status mutation, merge, rebase, force-push/ref rewrite, stale PR mutation, trading/risk/paper/live action, or no-auto-merge bypass is authorized.
- Required current-base identity: assigned worktree `HEAD`, `origin/main`, and merge-base must all equal `9ebaa9e7b44c61bb871ca4da0a838c52e62666b2`; branch/worktree must be `factory/zeus-alpha-research-ledger-core/inc-018-r2bn-canonical-g1-review-state-s` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-018-r2bn-canonical-g1-review-state-s`.
- Required canonical readback: approved `factory status` CLI from the assigned worktree must report Agent Core Postgres, source roots equal to the worktree, `factory_status_delegated=false`, 14/14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, active `reconciliation_anomalies=[]`, and stale primary rejected. The exact residual source must be recorded as stale structured R2ai/R2ae task metadata plus gates `927`/`928` and historical event projection, not current document content.
- Required deterministic validation: `python3 factory/projects/zeus-alpha-research-ledger-core/validate_r2bn_g1_evidence.py --status-json <status-json> --expected-base 9ebaa9e7b44c61bb871ca4da0a838c52e62666b2 --expected-head <final-pr-head-sha> --expected-pr https://github.com/SiteOneTech/hermes-agent-original/pull/80 --expected-quality-gate <quality-gate-id>` must fail before the R2bn artifact/index/gate/tracker evidence exists and pass only after a fresh PR and independent exact-SHA quality gate record are visible in canonical status.
- Delivery evidence is recorded: non-draft GitHub PR `https://github.com/SiteOneTech/hermes-agent-original/pull/80` against `main`, Zeus Signed-off-by commits, `agent:zeus` label, final head `5dcf7d14746457148b045e2ed94aed6114054e6d`, independent quality gate `929` by `quality-reviewer`, and merge commit `0db9bed7ed9e8ec4dbefda41f95a335ab82fbbc0`. This predecessor evidence does not authorize ALR-020/product dispatch.

## R2ct bounded canonical G1 documentation validation and PR-first recovery gate

- R2ct is limited to project-local documentation/evidence and the project-local validator under `factory/projects/zeus-alpha-research-ledger-core/`; no runtime/product code, Factory runtime code, deploy, credential, connector, messaging, direct SQL, external runtime, primary-checkout mutation, task-status mutation, merge, rebase, force-push/ref rewrite, stale PR mutation, trading/risk/paper/live action, or no-auto-merge bypass is authorized.
- Required current-base identity: assigned worktree `HEAD`, `origin/main`, and merge-base must all equal `0db9bed7ed9e8ec4dbefda41f95a335ab82fbbc0`; branch/worktree must be `factory/zeus-alpha-research-ledger-core/inc-019-r2ct-bounded-canonical-g1-docume` / `/home/jean/Projects/.worktrees/zeus-alpha-research-ledger-core/inc-019-r2ct-bounded-canonical-g1-docume`.
- Required canonical readback: approved `factory status` CLI from the assigned worktree must report Agent Core Postgres, source roots equal to the worktree, `factory_status_delegated=false`, 14/14 required G1 rows `exists/committed/indexed/validated/reviewed=true`, `blocking=false`, `readiness_source=configured_base_ref`, base `0db9bed7ed9e8ec4dbefda41f95a335ab82fbbc0`, active `reconciliation_anomalies=[]`, and stale primary rejected. The exact residual source must be recorded as stale structured R2ai/R2ae task metadata, not current document content.
- Required predecessor binding: PR #80 `https://github.com/SiteOneTech/hermes-agent-original/pull/80` head `5dcf7d14746457148b045e2ed94aed6114054e6d` passed independent quality gate `929` from base `9ebaa9e7b44c61bb871ca4da0a838c52e62666b2` and reached current `origin/main` as merge commit `0db9bed7ed9e8ec4dbefda41f95a335ab82fbbc0`.
- Required deterministic validation: `python3 factory/projects/zeus-alpha-research-ledger-core/validate_r2ct_g1_evidence.py --status-json <status-json> --expected-base 0db9bed7ed9e8ec4dbefda41f95a335ab82fbbc0 --expected-head <final-r2ct-pr-head-sha> --expected-pr <r2ct-pr-url> --expected-quality-gate <r2ct-quality-gate-id> --predecessor-base 9ebaa9e7b44c61bb871ca4da0a838c52e62666b2 --predecessor-head 5dcf7d14746457148b045e2ed94aed6114054e6d --predecessor-pr https://github.com/SiteOneTech/hermes-agent-original/pull/80 --predecessor-quality-gate 929` must fail before the R2ct artifact/index/gate/tracker evidence exists and pass only after a fresh PR and independent exact-SHA quality gate record are visible in canonical status.
- Delivery evidence must be PR-first: actual non-draft GitHub PR against `main`, Zeus Signed-off-by commit, `agent:zeus` label, immutable final candidate SHA in the PR body/Factory gate, base ancestry, G1 validation/status readback, explicit no-merge/no-direct-SQL/no-primary-mutation/no-force-push/no external runtime/no ALR-020 statement, and independent exact-SHA quality review before downstream dispatch relies on this repair. This worker must not self-approve or merge.
