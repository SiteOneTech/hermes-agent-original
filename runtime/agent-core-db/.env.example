# Local Zeus Agent Core DB runtime.
# Copy to .env and inject real secrets through Infisical/runtime for durable deployments.
AGENT_DB_ADMIN_USER=agent_admin
AGENT_DB_ADMIN_PASSWORD=replace-me
AGENT_DB_HOST_BIND=127.0.0.1
AGENT_DB_HOST_PORT=55430
AGENT_DB_NAME=zeus_agent
AGENT_DB_APP_USER=zeus_agent
AGENT_DB_APP_PASSWORD=replace-me
AGENT_DB_CALENDAR_NAME=nettu_calendar
